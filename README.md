# 🧮 Python Basics – Even/Odd Check & Sum of Numbers

This program performs two simple Python tasks:

## 1. Check if a Number is Even, Odd, or Zero
- Accepts a number from the user.
- Uses conditional statements (`if`, `elif`, `else`) to check:
  - If the number is **zero**.
  - If the number is **even**.
  - If the number is **odd**.
- Displays the appropriate message.

## 2. Calculate the Sum of Integers from 1 to 50
- Demonstrates two loop types:
  - **For Loop** – Uses `range(1, 51)` to iterate and add numbers.
  - **While Loop** – Uses a counter variable to sum numbers up to 50.
- Prints the total sum for both methods.

These exercises help understand **user input**, **conditional statements**, and **looping concepts** in Python.
